<?php
namespace Locomo;
class Help_Scdl
{
// index_admin
static $index_admin = <<< EOF

EOF;

// index_draft
static $index_draft = <<< EOF

EOF;

// edit_categories
static $edit_categories = <<< EOF

EOF;
}
