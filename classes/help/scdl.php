<?php
namespace Locomo;
class Help_Scdl
{
// calendar
static $calendar = <<< EOF

EOF;

// create
static $create = <<< EOF

EOF;

// viewdetail
static $viewdetail = <<< EOF

EOF;

// edit
static $edit = <<< EOF

EOF;

// attend
static $attend = <<< EOF

EOF;

// somedelete
static $somedelete = <<< EOF

EOF;

// regchange
static $regchange = <<< EOF

EOF;

// copy
static $copy = <<< EOF

EOF;

// delete
static $delete = <<< EOF

EOF;

// delete_others
static $delete_others = <<< EOF

EOF;
}
