<?php
namespace Locomo;
class Help_Scdl
{
// calendar
public $calendar = <<< EOF

EOF;

// create
public $create = <<< EOF

EOF;

// viewdetail
public $viewdetail = <<< EOF

EOF;

// edit
public $edit = <<< EOF

EOF;

// attend
public $attend = <<< EOF

EOF;

// somedelete
public $somedelete = <<< EOF

EOF;

// regchange
public $regchange = <<< EOF

EOF;

// copy
public $copy = <<< EOF

EOF;

// delete
public $delete = <<< EOF

EOF;

// delete_others
public $delete_others = <<< EOF

EOF;
}
