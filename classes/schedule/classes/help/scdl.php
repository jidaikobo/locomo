<?php
namespace Locomo;
class Help_Scdl
{
	static $txt = array(
		// edit
		'edit' => <<< EOF

EOF
		,

		// copy
		'copy' => <<< EOF

EOF
		,

		// attend
		'attend' => <<< EOF

EOF
		,

		// view
		'view' => <<< EOF

EOF
		,
	);
}
